<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">YelpCamp</a>
        <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarNav"
            aria-controls="navbarNav"
            aria-expanded="false"
            aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse navbar-nav" id="navbarNav">
            <a class="nav-link" href="/"><%=langData.home%></a>
            <a class="nav-link" href="/campgrounds"><%=langData.campgrounds%></a>
            <a class="nav-link" href="/campgrounds/new"><%=langData.newCamp%></a>
        </div>
        <div class="navbar-nav ml-auto">
            <% if(!currentUser) {%>
            <a class="nav-link" href="/login"><%=langData.login%></a>
            <a class="nav-link" href="/register"><%=langData.signUp%></a>
            <% } else {%>
            <a class="nav-link" href="/logout"><%=langData.logout%></a>
            <% }%>
        </div>
    </div>
</nav>
